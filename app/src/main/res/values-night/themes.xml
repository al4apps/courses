<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Courses" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.Courses" parent="Base.Theme.Courses">
        <item name="android:colorPrimary">@color/dark</item>
        <!--        <item name="colorSurface">@color/dark_grey</item>-->
        <item name="colorOnPrimary">@color/green</item>
        <item name="colorSurface">@color/green</item>

        <item name="android:colorBackground">@color/dark</item>

        <item name="bottomNavigationStyle">@style/BottomNavigationViewStyle</item>
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/dark_grey</item>
        <item name="itemIconTint">@drawable/bottom_nav_tint_selector</item>
        <item name="itemTextColor">@drawable/bottom_nav_tint_selector</item>
    </style>
</resources>